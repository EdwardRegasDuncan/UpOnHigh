extends Gun_Class
var isFiring

#input event to check when firing or not
#fire/reload functions
#fire function - fire raycast in direction of barrel - create vfx along raycast

func _fire():
#	if canFire == true:
#		var direction =- $MeshInstance3D/GunBarrel.global_transform.basis.z
#		var space_state = get_world_3d().direct_space_state
#		var ray_length = 10
#		var from_point = global_transform.origin
#		var to_point = global_transform.origin + direction.normalized() * ray_length
#		var ray_result = 
#		if ray_result:
#			var collision_point = ray_result.position
#			var collided_object = ray_result.collider
#			print(collided_object)
	pass
